pipeline {
  agent any
  tools {
    MAVEN 'Maven 3.8.7'
  }
  stages{
    stage('Build'){
      steps {
        sh "mvn --version"
        sh "mvn clean package -DSkipTests=true"
        archive 'target/*.jar'
      }
    }
  }
}

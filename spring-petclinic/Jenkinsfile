pipeline {
  agent any
  stages {
    stage('Build Image'){
      steps {                
        sh "docker build $WORKSPACE/spring-petclinic -t petc:local -f $WORKSPACE/spring-petclinic/Dockerfile"
      }
    }
  }
}

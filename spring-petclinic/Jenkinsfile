pipeline {
  agent any
  tools {
    maven 'Maven 3.3.9'
  }
  stages{
    stage('Build'){
      steps {
        sh "mvn --version"
        sh "mvn clean package -DSkipTests=true"
        archive 'target/*.jar'
      }
    }
  }
}
